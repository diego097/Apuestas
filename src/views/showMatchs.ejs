<div class="col-md-12">
    <div class="card">

        <%  finalJson.forEach(element => { %>
            <table class="table">

                <h2 class="text-center">
                    <%= element.market_name%>
                </h2>
                <thead>

                </thead>
                <tbody>
                    <tr>
                        <th scope="row"> </th>
                        <td>
                            <%= element.selection_name%>
                        </td>
                        <td>
                            <%= element.odd_quota%>
                        </td>
                        <td>
                            <%= element.selection_name%>
                        </td>
                        <td>
                            <%= element.odd_quota%>
                        </td>
                    </tr>

                </tbody>
            </table>
            <% }) %>
    </div>
</div>